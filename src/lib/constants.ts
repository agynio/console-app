export const NO_MODEL = 'none';

// Radix Select cannot hold an empty string as a value, so "unset" needs a
// sentinel that is mapped back to an empty id on submit.
export const NO_ENVIRONMENT = 'none';
